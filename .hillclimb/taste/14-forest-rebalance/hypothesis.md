# 14: Forest rebalance

Authority: user-endorsed forest plan. No CloudAgent/PR. Execute on :4500 craft.

## Hypothesis

Reorder page to **Hero → EncounterStrip → Orbit → Memory → FinalCta**, unmount Evidence (file kept), and retarget Memory to a **macro-loop only** (six-month trajectory: accumulation → model independence → learner agency) so cold scroll past Encounter does **not** feel like watching the movie again. Organic bridge after EncounterBeat 6 unpin into Orbit: *One encounter for one target. Now your whole syllabus held to this standard.*

## Scope

- `page.tsx` reorder + Evidence unmount
- Orbit intro bridge (not a third sticky)
- Memory rewrite (content + craft visuals); cut docket/ledger/≠ bar echoes
- Nav Method/Moves/Memory hrefs unchanged (`#method` `#moves` `#memory`)
- Out of scope: Hero, ScrollCraft engine, EncounterStrip beat re-litigation

## Freeze impact

`content.ts` memory block rewritten → global `.hillclimb/taste/frozen.json` content hash will fail until re-frozen with authority. Hero.tsx and ScrollCraft assets untouched. Bridge copy is Orbit-local (avoids extra freeze churn beyond Memory).

## Falsifier

Cold scroll past Encounter must NOT feel like watching the movie again in Memory.
