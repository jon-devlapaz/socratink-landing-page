/**
 * Unmounted (forest spine post–14). Evidence card data removed from content.ts
 * in candidate 18-parsimony-words. File kept on disk for archive/reference.
 */
export function Evidence() {
  return null;
}
