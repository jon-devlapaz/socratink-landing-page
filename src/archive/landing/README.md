# Archived landing pieces

Kept for insight (Jonathan, 2026-09-13). These ran on the homepage or as diagnostic routes before Phase 6 cut the prospectus to title → 01 map → 02 speak → 03 keep → colophon → footer.

The live homepage and app routes must not import this folder.

**What is here**
- Orbit (subject rings) and its CSS
- Retention curve / specimens
- Memory ledger
- FAQ
- Final CTA (recovered from git)
- Old How-it-works walkthrough (`StepRail`, `StepPreview`, `steps.ts`, scroll-progress hook)
- OrganicSphere and `sphere/` (unused by the kept blob)
- Trailing ink cursor
- Section heading / reveal helpers used by leftover sections
- `/positioning` diagnostic page
- `/ink-lab-v2` workshop page (`/ink-lab` stays live and unlinked)
- Unused `useActProgress` hook, leftover capture/verify scripts, and unused `scrollcraft.js` (`src/archive/scripts/`, `src/archive/scroll/`)

Old hill-climb and Method docs live in `docs/archive/`.
