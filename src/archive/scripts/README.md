# Archived leftover scripts and modules

One-off capture/verify scripts and unused scroll helpers from earlier landing experiments. The live homepage and `package.json` scripts must not import this folder.

`scrollcraft.js` is the unused engine copy; live pages still use `public/scrollcraft/scrollcraft.css`.
